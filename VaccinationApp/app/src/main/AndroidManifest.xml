<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.iprd.vaccinationapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".GoogleLogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ScanVaccineBatch" />
        <activity android:name=".AutoCaptureScanner" />
        <activity android:name=".Badge" />
        <activity android:name=".LocationConfirmation" />
        <activity android:name=".PinLogin" />
        <activity android:name=".CuponValidation" />
        <activity android:name=".RecordDoseOrReportSideEffect" />
        <activity android:name=".ReadyToScanToday" />

    </application>

</manifest>